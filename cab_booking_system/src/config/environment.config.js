module.exports = {
    development: {
        db: {
            HOST: "localhost",
            PORT: 27017,
            DB: "projectDb"
        }
    },
    production: {
        db: {
            HOST: "localhost",
            PORT: 27017,
            DB: "projectDb"
        }
    }
}